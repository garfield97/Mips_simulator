#include "father.cpp"
#include "instructions.hpp"
#include <string>

using namespace std;


int main()
{
    string instruction, type;
    int rs, rt, rd, IMM, shift;
    while(instruction != "exit")
    {
        
    }
}