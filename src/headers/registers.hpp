#ifndef REGISTERS_HPP
#define REGISTERS_HPP

#include <iostream>

typedef  int reg;

class registers
{
    public:
    
    int reg[32];
    int hi;
    int lo;
    // Declare each register in one class

    
    registers(); //function to initialise
};


#endif
